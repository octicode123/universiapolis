import React from 'react';

const Header = () => (
    <header className="bg-primary text-white text-center py-3">
        <h1>Image and Weather App</h1>
    </header>
);

export default Header;